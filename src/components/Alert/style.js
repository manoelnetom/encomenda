import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  container: {
    fontSize: '1.7rem',
    fontWeight: 'lighter',
  },
  root:{
    borderRadius:'0rem'
  }
}));

export default useStyles;
